//function that takes an array as an input to check a particular text is available, if the text is available, the content is returned



function arraydata(array){

  var return_value; 
  
   array.forEach(function(mail){
                     
                     if(mail[0] == "YEM")
                     {
                     return_value= mail[0]}
                   }); 
return return_value
  }
